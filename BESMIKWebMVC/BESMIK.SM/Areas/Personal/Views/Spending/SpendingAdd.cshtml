@model SpendingViewModel
@{
    Layout = "_Layout";
}

<div class="pagetitle">
    <h1>Harcama Ekle</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Spending" asp-action="SpendingAdd">Harcama Ekle</a></li>
            <li class="breadcrumb-item">Harcama İşlemleri</li>
            <li class="breadcrumb-item active">Harcama Ekle</li>
        </ol>
    </nav>
</div>

<section class="section profile">
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body pt-3">
                    <div class="tab-content pt-2">
                        <form asp-controller="Spending" asp-action="SpendingAdd" method="post" enctype="multipart/form-data">

                            <div asp-validation-summary="ModelOnly"></div>

                            <div class="form-group">
                                <label asp-for="Picture"> </label>
                                <input asp-for="Picture" type="file" class="form-control" />
                            </div>
@*                             <div class="row mb-3">
                                <label for="profileImage" class="col-md-4 col-lg-3 col-form-label">Fotoğraf</label>
                                <div class="col-md-8 col-lg-9">
                                    <!-- arkaplan resmi -->
                                    <img id="profileImagePreview" src="@(Model.SpendingFile ?? "/img/imageupload1.jpg")" alt="Profile Image" style="max-width: 150px; max-height: 150px;" />

                                    <!-- ekleme ve silme  -->
                                    <div class="pt-2">
                                        <a href="javascript:void(0);" class="btn btn-primary btn-sm" title="Upload new profile image" onclick="document.getElementById('uploadImageInput').click();">
                                            <i class="bi bi-upload"></i>
                                        </a>
                                        <a href="javascript:void(0);" class="btn btn-danger btn-sm" title="Remove my profile image" onclick="removeProfileImage();">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>

                                    <!-- Resim yükleme -->
                                    <input type="file" id="uploadImageInput" name="FormFile" style="display: none;" onchange="previewImage(this);" />
                                    <span asp-validation-for="Picture" class="form-text text-danger"></span>
                                </div>
                            </div> *@


                            <div class="row mb-3">
                                <label for="spendingType" class="col-md-4 col-lg-3 col-form-label">Harcama Türü</label>
                                <div class="col-md-8 col-lg-9">
                                    <select name="SpendingType" class="form-select">
                                        <option value="">Bir harcama türü seçiniz</option>
                                    </select>
                                    <span asp-validation-for="SpendingType" class="form-text text-danger"></span>
                                </div>
                            </div>

                            <div class="row mb-3">
                                <label for="sum" class="col-md-4 col-lg-3 col-form-label">Harcama Tutarı</label>
                                <div class="col-md-8 col-lg-9">
                                    <input name="Sum" type="text" class="form-control" id="sum" value="@Model.Sum">
                                    <span asp-validation-for="Sum" class="form-text text-danger"></span>
                                </div>
                            </div>

                            <div class="row mb-3">
                                <label for="spendingCurrency" class="col-md-4 col-lg-3 col-form-label">Para Birimi</label>
                                <div class="col-md-8 col-lg-9">
                                    <select name="SpendingCurrency" class="form-select">
                                        <option value="">Bir para birimi seçiniz</option>
                                    </select>
                                    <span asp-validation-for="SpendingCurrency" class="form-text text-danger"></span>
                                </div>
                            </div>


                            <div class="text-center">
                                <button type="submit" class="btn btn-primary">Değişiklikleri Kaydet</button>
                            </div> 
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            // sectiona harcama türlerini yükleme
            $.get("@Url.Action("GetSpendingType", "Spending")", function (data) {
                var select = $("select[name='SpendingType']");
                select.empty();
                select.append('<option value="">Bir harcama türü seçiniz</option>');
                $.each(data, function (index, item) {
                    select.append($('<option>', {
                        value: item.value,
                        text: item.text
                    }));
                });
            });

            // sectiona para birimleri yükleme
            $.get("@Url.Action("GetSpendingCurrency", "Spending")", function (data) {
                var select = $("select[name='SpendingCurrency']");
                select.empty();
                select.append('<option value="">Bir para birimi seçiniz</option>');
                $.each(data, function (index, item) {
                    select.append($('<option>', {
                        value: item.value,
                        text: item.text
                    }));
                });
            });

        });
    </script>

@*     <script>
        // resim secme için
        function previewImage(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById('profileImagePreview').src = e.target.result;
                }
                reader.readAsDataURL(input.files[0]);
            }
        }

        // seccilen resmi kaldırmak için
        function removeProfileImage() {
            document.getElementById('profileImagePreview').src = '/img/imageupload1.jpg';
            document.getElementById('uploadImageInput').value = '';
        }
    </script> *@

}