@using BESMIK.ViewModel.Permission
@model List<PermissionViewModel>

@{
    Layout = "_PersonalLayout";
}
@* <div class="pagetitle">
    <h1>İzin Talebinde Bulun</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="PersonalAppUser" asp-action="Summary">Anasayfa</a></li>
            <li class="breadcrumb-item">Şirket Yönetici İşlemleri</li>
            <li class="breadcrumb-item active">Yönetici Listele</li>
        </ol>
    </nav>
</div> *@

<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">İzin Listesi</h5>

                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th>İzin Türü</th>
                                <th>İzin Başlangıç Tarihi</th>
                                <th>İzin Bitiş Tarihi</th>
                                <th>İzin Gün Sayısı</th>
                                <th>İzin Durumu</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (PermissionViewModel item in Model)
                            {
                                <tr>
                                    <td>@item.PermissionType</td>
                                    <td>@item.PermissionStartDate</td>
                                    <td>@item.PermissionEndDate</td>
                                    <td>@item.OffDaysNumbers</td>
                                    <td>@item.PermissionStatus</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>

        </div>
    </div>
</section>